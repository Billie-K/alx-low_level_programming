#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
* main - a progrmam that prints args
* @argc: the number of args
* @argv: the arguments to print
* Return: 0 if successful
*/
int main(int argc, char **argv)
{
int count;

for (count = 0; count < argc; count++)
{
printf("%s\n", argv[count]);
}
return (0);
}
